---
title: "Hello World"
date: "2026-03-30"
summary: "My first blog post -- testing syntax highlighting with Shiki."
tags: ["meta", "typescript"]
published: false
---

This is my first blog post. Here is some highlighted TypeScript:

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`
}

console.log(greet("World"))
```

And some C# since that is what I work with:

```csharp
public class Greeter
{
    public string Greet(string name) => $"Hello, {name}!";
}
```

And a quick bash snippet:

```bash
echo "Hello from the terminal!"
```
